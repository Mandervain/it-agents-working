---
name: example-command
description: Template for a custom slash command. Replace with real commands.
---

# Example Command

<!-- TODO: Replace with a real slash command.

Custom commands let you define reusable prompts invoked with /command-name.
The file name (without .md) becomes the slash command name.

Example — save as .claude/commands/review.md:

---
name: review
description: Review the current branch diff for correctness and style issues.
---

Review the output of `git diff main` for:
- Logic errors or off-by-one bugs
- Missing error handling at system boundaries
- Style violations against the conventions in CLAUDE.md
- Security issues (injection, exposed secrets, missing auth checks)

Report findings as a bulleted list grouped by severity: Critical / Warning / Suggestion.
-->
