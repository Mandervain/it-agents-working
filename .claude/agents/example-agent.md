---
name: example-agent
description: >
  Template for a sub-agent definition. Replace this file with real agents.
  Sub-agents are spawned by Claude to handle focused, isolated subtasks.
  Example use cases: code reviewer, test runner, documentation generator.
tools:
  - Read
  - Bash
  - Edit
---

# Example Agent

<!-- TODO: Replace with a real agent.

A sub-agent file defines a specialized role Claude can adopt when handling a
subtask. Describe:

1. What this agent's job is (one sentence)
2. What tools it may use
3. What inputs it expects
4. What output it should return

Example:

---
name: test-runner
description: Runs the test suite and reports failures with file:line references.
tools: [Bash, Read]
---

Run `npm test` and report any failing tests with the exact file path and line
number. Do not attempt to fix the failures — just report them clearly.
-->
